# Playwright package + recordings (untracked)
/e2e-testing/

# Playwright / test output (repo root or elsewhere)
/test-results/
/playwright-report/
/blob-report/
**/.playwright/

# Curated demo exports from local E2E runs (untracked)
/demo videos/

# merchant-pos: Preprod desk / Playwright-targeted UI (keep local only; not versioned)
/merchant-pos/src/pages/e2e-demo.tsx
/merchant-pos/src/pages/e2e-demo/
/merchant-pos/src/components/e2e/
/merchant-pos/src/pages/api/e2e/
/merchant-pos/src/server/wallet-demo.ts

# Next.js build output + per-process caches
merchant-pos/.next/
merchant-pos/tsconfig.tsbuildinfo
merchant-pos/data/

# OS / editor
.DS_Store

# Dependencies (install locally)
node_modules/

# Claude Code config
.claude/

# Temp Playwright recordings
docs/demo-videos/*.webm

# MkDocs build output (CI re-renders on push)
/site/

# Live docker env files (.example templates are committed)
docker/.env.preprod
docker/.env.preview
docker/.env.mainnet

# Orchestrator-generated per-head compose files (one per Hydra head)
/docker/heads/*.yml
/docker/heads/*.yml.bak

# Cardano chain data + Hydra runtime state (huge, host-mounted)
/data/
/hydra-config/keys/
/hydra-config/cardano-data/

# Public mainnet config files we fetch via scripts/fetch-cardano-mainnet-config.sh
# (the only file we actually vendor is protocol-parameters.json, which is small)
/hydra-config/alonzo-genesis.json
/hydra-config/byron-genesis.json
/hydra-config/conway-genesis.json
/hydra-config/shelley-genesis.json
/hydra-config/config-mainnet.json
/hydra-config/topology-mainnet.json
/hydra-config/checkpoints.json
